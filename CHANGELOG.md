# 更新日志

面向用户的版本变化记录。新条目先进顶部「未发布」段，发版时定稿为版本段；纯开发向改动（测试、脚本、CI、文档）不记录。约定细节见 `AGENTS.md` 编辑约定的「更新日志」条。

## 4.1.4 - 2026-09-20

- 新增：软件版本检测。启动后静默查一次发布页，设置里也可随时手动检查；查到新版时侧边栏 / 设置 Tab 的设置入口挂一个红点，更新那一行给出「前往下载」（打开发布页，应用内不自动替换安装）。查询失败如实说明原因，不会把「查不了」说成「已是最新」；当前平台不支持时同样直说。桌面与移动端共用
- 新增：检查结果直接写在设置面板的「更新」那一行里——查完把「检查更新」换成结果本身（绿色「已是最新版本」/ 新版本号 + 发布日期），更新说明可就地展开；失败时那行写明原因并换成「重试」（查不到发布版或平台不支持时不给重试，点了也是同样结果），不再只弹一句一闪而过的提示
- 修复：「关于」与移动端设置里显示的版本号不再与安装包不符——此前是手写常量，pubspec 已到 4.1.3 而界面仍显示 4.0.1；现在改为启动时读宿主包信息
- 界面：设置面板与新建 / 编辑连接表单的说明文字大幅精简——删掉重复的说明（开关名已经说清的不再配一句）、把长句压成一句，只保留有后果或有技术词的提示（安全警告、备份口令、`ssh-rsa` / `ssh-add` 这类要用来搜索的词）；示例性占位符（「例：web-prod-01」）去掉，`IP 或域名` 这类消歧义的保留。桌面与移动端共用
- 变更：终端「选中即复制」改为默认打开（终端里选中文字几乎总是为了复制，多按一次 ⌘C 是多余动作）。升级后已经装过本应用的也会跟着打开一次；此后你在「设置 → 终端」里的开关选择会被记住，不再被默认值改动覆盖

## 4.1.3 - 2026-09-20

- 修复：编辑已连接的主机（如改备注）后，顶部状态胶囊错误显示「未连接」——编辑保存不再重置会话层的运行时状态；桌面与移动端共用

## 4.1.2 - 2026-09-20

- 界面：浅色主题的状态点与状态胶囊改用 GitHub light 配色，白底对比度不再低于 WCAG 3:1（深浅两端观感不变，仅浅色下状态色更深）；桌面与移动端共用

## 4.1.1 - 2026-09-19

- 移动端：主机详情页终端 Tab 未连接时改为与 SFTP Tab 一致的引导空态（图标 + 提示），不再显示黑终端预览画面；桌面端保持终端样式预览不变
