# 更新日志

面向用户的版本变化记录。新条目先进顶部「未发布」段，发版时定稿为版本段；纯开发向改动（测试、脚本、CI、文档）不记录。约定细节见 `AGENTS.md` 编辑约定的「更新日志」条。

## 4.4.0 - 2026-09-23

- 新增：SFTP 文件列表给 `.cnf`（如 MySQL 的 my.cnf）和 systemd 的 `.service` 补上配置齿轮图标，此前这两类后缀显示为通用白色文件图标。桌面与移动端共用
- 新增：连接 Windows（Win32 OpenSSH）时，SFTP 地址栏下方的快捷胶囊给出「~ + 盘符」——自动探测远端全部盘符，点一下直达对应盘的根；此前 Windows 远端一行胶囊都不显示，换盘只能手动敲路径。桌面与移动端共用
- 优化：Windows 远端的 SFTP 地址栏进入编辑态时预填 `C:\Users` 原生盘符写法，复制出去贴进资源管理器、终端不再带着 SFTP 内部形态的开头斜杠（`/C:/Users`）；原生写法贴回地址栏照样前往，边敲边补也认 `\` 分隔。桌面与移动端共用
- 修复：macOS 上「记住凭据」可能整体失效——聚合档内容损坏时，还未迁移进聚合条目的旧凭据会跟着读不出来，保存也一直失败；现在损坏只影响聚合档自己，旧条目照常读回并在使用时迁移，保存会按空档重建。仅 macOS
- 修复：SFTP 文件列表快速连点鼠标侧键「后退」时，后退历史会被一次载入连退两格，之后「前进」回到的目录和画面错位。桌面端

## 4.3.0 - 2026-09-22

- 新增：终端复制的生产力三件套——双击选词整条带走 UUID、文件路径、域名（`-` `.` `:` `/` 不再断词，双击日志编号一下选中一整条）；有选区时 Ctrl+C 直接复制、没有选区仍然是中断（Windows Terminal 的默认语义，不用再按 Ctrl+Shift+C）；拖选到屏幕上下边缘画面跟着滚，超过一屏的内容也能选完。另加 X11 老键位 Shift+Insert 粘贴、Ctrl+Insert 复制。桌面与移动端共用
- 新增：聚焦的终端光标按 530ms 周期闪烁；远端声明 blink 时同样跟随，输出会重置节拍，失焦后回到可见相位。桌面与移动端共用
- 新增：SFTP 地址栏下方提供 `/`、家目录与常用系统目录的快捷胶囊；目录历史支持鼠标侧键前进 / 后退，快捷连接和 Tab 切换后终端自动接管焦点。桌面端
- 界面：SFTP 文本预览改用当前终端字体家族、行号槽和代码编辑器式排版；预览底色改为跟随浅色 / 深色主题，语法高亮配色也按主题分别校准。桌面与移动端共用
- 修复：选中文字像打了马赛克——选区的实心矩形原是画在文字之上的，把选中的字整片盖住；现在选区改画在文字之下当背景层，选中的字以原色清晰可见（GNOME / Windows Terminal 的画法），各配色的选区色同步校准（GitHub Dark / Light 两套此前为此压成 15% 透明的选区改回实色）。桌面与移动端共用
- 修复：Linux / Windows 下 Ctrl+A 被终端「全选」抢走，shell 里按不出 readline 的回行首（^A）——没有任何主流终端绑这个，现在 Ctrl+A 照常进 shell，全选仍走右键菜单。桌面端
- 修复：macOS 钥匙串中「每台主机一个凭据条目」会随着常用主机越积越多；新构建因签名身份变化逐条索要授权。现在 macOS 凭据放进同一个聚合条目，新构建最多授权一次；旧分条档按需迁移，单台主机凭据仍可独立读取、更新与删除。桌面端

## 4.2.1 - 2026-09-22

- 新增：SFTP 快速预览支持代码语法高亮。常见源码、标记、配置与 Shell 脚本按扩展名解析成关键字、字符串、注释等彩色文本；超大行仍按块懒加载，超过 1 MB 的预览文本自动回落纯文本，避免「看一眼」变成一次整文件排版。桌面与移动端共用
- 修复：`.env` 这类以点开头的配置文件此前会被扩展名判断误排除，现在可以进入快速预览并按 Shell 语法高亮。桌面与移动端共用

## 4.2.0 - 2026-09-21

- 新增（内测）：SFTP 快速预览。常见图片可直接查看并缩放；文本、代码、配置和日志按行懒加载，可见内容可选中复制。默认上限 16 MB，可在「设置 → 连接 → 快速预览上限」调整为 4 / 16 / 64 / 256 MB。该功能仍处于内测阶段，超限或解析失败会保留原有下载入口，不会改动远端文件。桌面与移动端共用
- 界面：快速预览使用半透明深色背景，保留下层界面的一点上下文；macOS 上返回箭头移到红绿灯右侧，Windows / Linux 维持原位。桌面与移动端共用
- 新增：SFTP 文件列表的图标换成 vscode-icons 的彩色类型图标（MIT，随包内置约 40 种常用图形，覆盖语言脚本、图片、音视频、压缩包、数据库、密钥、Docker / Git 文件等），认不出的扩展名回落通用文件图标；目录与软链接保持原主题色图形——「蓝色文件夹 = 可进入」的信号不变。桌面与移动端共用
- 修复：SFTP 地址栏在家目录之外的路径上出现「/ /」双斜杠——根段的斜杠本身就是分隔符，段间不再重复画一条（与 GNOME 文件管理器一致）。桌面与移动端共用
- 界面：SFTP 列表选中行与侧边栏当前选中主机的底色加深（primary 覆盖由浅色 12% / 深色 20% 提到 18% / 26%）——两处共用同一语义色，选中态一眼可辨。桌面与移动端共用
- 界面：SFTP 数据库文件（.sql / .db / .sqlite）的图标从「服务器机架」换成「数据集」图形，与文本、代码、配置等类别一样看形状就能认出来。桌面与移动端共用
- 界面：SFTP 列表的过界果冻回弹压到约默认的十分之一——iOS / macOS 上滚到头只给一丝让步就收回，不再整页大幅弹动；文件列表与传输队列统一，六端同一观感。桌面与移动端共用

## 4.1.6 - 2026-09-21

- 修复：Linux 下用中文输入法（fcitx5 / ibus）打一个字上屏三次——引擎会把同一笔上屏连发三遍，而终端按写死的初始状态算差集，于是每一遍都被当成新输入发往远端；现在按平台最近一次报来的文本算，只有真正的新内容才发出去（同一个字连打两次照发两遍）。桌面端
- 修复：SFTP 地址栏编辑态「框里套框」——输入框自己又铺了一层主题的灰底与聚焦蓝边（主题的 `enabledBorder` / `focusedBorder` 会压过 `border: none`），与外层地址栏框叠成两个圆角矩形。现在编辑态只剩一层外框，与浏览态完全一致。桌面与移动端共用
- 界面：SFTP 地址栏与 GNOME 文件管理器（Nautilus）的位置栏对齐——家目录折成一段「主目录」，每段粗体、段间一个暗色斜杠，点当前段或空白处进编辑态；路径装不下改成横向滚动（竖直滚轮也滚得动）并自动把当前目录滚进视野，超长的一层中间省略。此前是横向滚动把开头藏掉、中间几层收进「…」菜单。桌面与移动端共用
- 新增：SFTP 地址栏认得 `~` 与相对路径（`~/logs`、`../etc` 都能去），并且边敲边补——候选下拉跟着输入收窄，能确定的那截自动补上并选中，上下键预览候选、回车先收下候选再前往、Esc 先收下拉。桌面与移动端共用
- 新增：点过 SFTP 面板之后按 Ctrl/⌘+L 直接把地址栏叫进编辑态（GNOME 文件管理器的键位），此前只能先找准那块地方点一下。桌面端
- 修复：移动端 SFTP 列表里上下滑动会「点进」手指落下的那个文件夹——目录 / 文件的进入与选中从「按下即触发」改成「抬手且没滑动才算」，与列表滚动在手势竞技场里裁决：滑动、甩动、以及按住列表停住惯性，都不再误进目录或误选文件（平板 / 折叠屏这类宽屏触屏设备同样按手指处理，鼠标仍然按下即选中）。触屏单击也因此不再被双击手势扣住 300ms，抬手即响应。移动端
- 界面：移动端 SFTP 的目录行行尾多了一个「点进去」的箭头——同一行上「点目录进下一层、点文件是多选」的区别此前只能靠试。移动端
- 界面：移动端快捷键条收窄了键帽（落点仍给足 40×40），一行里能同时看到 esc / tab / ctrl / alt 与方向键——此前 390pt 的屏上方向键要横滑才看得见。移动端
- 修复：终端查找栏的输入框在浅色主题下几乎看不见——输入框铺的是应用主题的白底，文字色却来自终端配色（浅灰）。现在输入框自己铺终端底色。桌面与移动端共用
- 新增：移动端终端支持双指捏合缩放字号——手势期间只在画面上浮一个「字号 15」的预览，抬手才真正生效（字号一变远端就要重排，逐帧改会让 vim / tmux 跟着抖）。键条上的 A- / A+ 仍在。移动端
- 新增：移动端快捷键条上多了一颗「鼠标」开关——远端程序自己开了鼠标上报时（vim 的 `set mouse=a`、tmux 的 `mouse on`）它才亮起来，打开后手指拖动就是远端的鼠标拖动：vim 里拉可视选区、tmux 里拖分割线、htop 里点列头。关掉、或远端并没有在收鼠标事件时，拖动照旧滚动画面。此前触屏对这些程序只能点、不能拖。移动端
- 新增：移动端选中文字后出现首尾两颗选区手柄，可以拖着改选区；手指拖出终端上下边界时画面跟着滚，选区一直延伸到边上那一行（此前只能按住不动地拖，拖出屏幕就没了下文）。移动端
- 修复：终端查找在远端清屏 / 重排之后会跳错行，甚至越界报错——现在步进前重算一遍，输出时按 300ms 节流跟进（保持当前那一条、不把画面拽走）。桌面与移动端共用
- 新增：终端里可以查找了——工具栏上的放大镜（外接键盘是 ⌘F，其余平台 Ctrl+Shift+F，避开 shell 自己的 Ctrl+F）展开查找栏，在回滚缓冲里找文本，大小写不敏感；命中全部上底色、当前那一处颜色更实，配「上一个 / 下一个」与命中计数，命中在视野之外会把画面滚过去。桌面与移动端共用
- 新增：移动端快捷键条上多了 A- / A+ 两颗字号键，就地放大缩小终端字号（与设置页的步进、外接键盘的 Cmd/Ctrl 加减是同一个值，一样会记住）。移动端
- 界面：全屏终端页不再常驻顶部那条 AppBar，改成浮在终端之上的一条头部——进页面先亮 3 秒，之后自动收起；轻点顶部一条唤出，点终端正文立刻收回。省下的那一行高度在键盘弹起、只剩几行可见时尤其要紧。头部里仍是返回、主机名、状态胶囊（点开是会话日志，多开了才换成会话菜单）与断开。移动端
- 新增：终端滚上去看历史后，右下角会出现一颗「回到最新」。此前新输出不会把画面拽回底部（这是刻意的：看到一半被拽走更烦），但也没有一键回底的办法；触屏端另加了一条滚动位置指示（桌面端的滚动条由框架自带）。桌面与移动端共用
- 修复：移动端在同一台主机上开第二条会话此前没有入口——「新建会话」只挂在桌面端，全屏终端里也切不了会话。现在主机详情页与全屏终端页顶部的状态胶囊点开就是会话菜单：多条会话时胶囊显示条数，点开可切换 / 新建 / 看日志；只有一条时点开仍是会话日志，外观与从前完全一样。桌面与移动端共用同一条规则。移动端
- 新增：移动端终端在软键盘上方多了一条快捷键条——Esc / Tab / Ctrl / Alt / 方向键。Ctrl 与 Alt 是粘滞的：点一次只对下一个按键生效（再点一次取消）；触屏上按不出「按住 Ctrl 再按字母」，而在此之前手机上敲不出 Ctrl+C 中断命令，也没法用 Tab 补全路径、用方向键翻历史、用 Esc 退出 vim 的插入模式。键条可折叠，窄屏可横向滑动，按键后焦点留在终端（软键盘不会自己收起）。移动端
- 修复：移动端终端里点不开链接、也唤不出复制 / 粘贴菜单——此前链接要求「鼠标 + 按住 Cmd/Ctrl」两个条件同时成立，触屏一个都满足不了；终端菜单同理，只有鼠标右键能弹出来，触屏上唯一的复制入口是右上角那颗 28 见方的悬浮按钮。现在触屏单击链接直接打开（双击仍是选词；vim / less 这类全屏程序里点按照旧交给远端应用），长按弹出菜单：复制 / 粘贴 / 全选，按在链接上时多一项「打开链接」，悬浮工具条的按钮在触屏上放大到 44。移动端
- 界面：移动端设置里的版本号不再显示两次——「关于」行去掉重复的 `NoShell v4.1.6` 副标题，版本号只留在「更新」那一行（与桌面端同一处），点进「关于」仍然看得到。移动端
- 修复：Android（小米 / 澎湃 HyperOS）上选择 SSH 密钥文件时「看不到、点不中」没有扩展名的私钥（`id_rsa` / `id_ed25519`）——这类文件在系统里是 `application/octet-stream`，而澎湃的「安全访问」选择器按 MIME 分类展示，不声明这一类的文件就被藏起来；现在选择器会显式声明 `*/*` / `application/octet-stream` / `text/plain`，让这类文件重新可选。仍然读不出来时，改为弹出带替代做法的说明框（把密钥重命名加上 `.txt` 再选，或用文件管理器打开后粘贴内容），并写明本应用只按次读取你选中的那一个文件、不申请整机存储访问权限。移动端

## 4.1.5 - 2026-09-20

- 新增：主机右键菜单多了「复制主机信息」——按主机文本格式（名称 / 地址 / 端口 / 用户 / 密码）写进剪贴板，密码取该主机已记住的凭据，没记住就留一行空的「密码: 」；粘贴出去既能给人看，也能直接粘回导入框。桌面端

## 4.1.4 - 2026-09-20

- 新增：软件版本检测。启动后静默查一次发布页，设置里也可随时手动检查；查到新版时侧边栏 / 设置 Tab 的设置入口挂一个红点，更新那一行给出「前往下载」（打开发布页，应用内不自动替换安装）。查询失败如实说明原因，不会把「查不了」说成「已是最新」；当前平台不支持时同样直说。桌面与移动端共用
- 新增：检查结果直接写在设置面板的「更新」那一行里——查完把「检查更新」换成结果本身（绿色「已是最新版本」/ 新版本号 + 发布日期），更新说明可就地展开；失败时那行写明原因并换成「重试」（查不到发布版或平台不支持时不给重试，点了也是同样结果），不再只弹一句一闪而过的提示
- 修复：「关于」与移动端设置里显示的版本号不再与安装包不符——此前是手写常量，pubspec 已到 4.1.3 而界面仍显示 4.0.1；现在改为启动时读宿主包信息
- 界面：设置面板与新建 / 编辑连接表单的说明文字大幅精简——删掉重复的说明（开关名已经说清的不再配一句）、把长句压成一句，只保留有后果或有技术词的提示（安全警告、备份口令、`ssh-rsa` / `ssh-add` 这类要用来搜索的词）；示例性占位符（「例：web-prod-01」）去掉，`IP 或域名` 这类消歧义的保留。桌面与移动端共用
- 变更：终端「选中即复制」改为默认打开（终端里选中文字几乎总是为了复制，多按一次 ⌘C 是多余动作）。升级后已经装过本应用的也会跟着打开一次；此后你在「设置 → 终端」里的开关选择会被记住，不再被默认值改动覆盖

## 4.1.3 - 2026-09-20

- 修复：编辑已连接的主机（如改备注）后，顶部状态胶囊错误显示「未连接」——编辑保存不再重置会话层的运行时状态；桌面与移动端共用

## 4.1.2 - 2026-09-20

- 界面：浅色主题的状态点与状态胶囊改用 GitHub light 配色，白底对比度不再低于 WCAG 3:1（深浅两端观感不变，仅浅色下状态色更深）；桌面与移动端共用

## 4.1.1 - 2026-09-19

- 移动端：主机详情页终端 Tab 未连接时改为与 SFTP Tab 一致的引导空态（图标 + 提示），不再显示黑终端预览画面；桌面端保持终端样式预览不变
