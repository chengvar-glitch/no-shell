<svg xmlns="http://www.w3.org/2000/svg" width="1024" height="1024" viewBox="0 0 1024 1024">
  <!-- artwork only. background plates live in render.py, which wraps this into
       the app icon, the maskable icon and the Android adaptive layers.

       NShell mark: the N is what you typed, the block cursor after it is the
       live shell. Strokes and the cursor share the same rounded geometry, and
       the letterform is centred so the adaptive-icon safe zones hold. -->
  <!-- artwork -->
  <path d="M208 730 V294 L628 730 V294" fill="none" stroke="#EAF0FB" stroke-width="120" stroke-linecap="round" stroke-linejoin="round"/>
  <rect x="732" y="650" width="144" height="140" rx="34" fill="#4C8DFF"/>
</svg>
